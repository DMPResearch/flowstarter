# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
# Generated by `pnpm effects:manifest`, and byte-compared by the suite: a
# formatter rewriting them would make `--check` fail against its own writer.
apps/flowstarter-templates/*/effects.json
